

<div class="container fixed-top offset-9 col-3">
    <ul class="list-group">
        <h1 style="color: white">Catégories </h1>
        {% for category in categories %}
            <a href="{{ path('category_articles', {'id': category.id}) }}" class="list-group-item d-flex justify-content-between align-items-center">
                {{ category.name }}
            </a>
        {% endfor %}
    </ul>
</div>