{% extends 'base.html.twig' %}

{% form_theme formCategory 'bootstrap_4_layout.html.twig' %}

{% block body %}

    <h1>Création d'une catégorie !</h1>

    {{ form_start(formCategory) }} {# <---- pas obligatoire #}

    {{ form_row(formCategory.name, {'attr': {'placeholder': "Nom de la catégorie" }}) }}

    <button type="submit" class="btn btn-success">Ajouter</button>

    {{ form_end(formCategory) }} {# <---- pas obligatoire #}

{% endblock %}